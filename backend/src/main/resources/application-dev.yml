spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/motors_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.motors: DEBUG
    org.hibernate.SQL: DEBUG
